# Transformation Rules

Per-field-type normalization rules will be stored here:
- Phone number formatting (E.164 normalization)
- Date format standardization
- Email normalization
- Name/text casing and whitespace cleanup

Each rule is independently testable and pure (no cross-row dependencies).
