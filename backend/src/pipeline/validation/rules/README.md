# Validation Rules

Per-field-type validation rules will be stored here:
- Schema validation (required fields, type checks)
- Business validation (format patterns, range checks)
- Email format validation (RFC-5322)
- Phone number validation
- Date validation

Each rule is independently testable and pure (no cross-row dependencies).
