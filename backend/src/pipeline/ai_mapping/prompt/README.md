# AI Mapping Prompt Templates

This directory will contain:
- Prompt segment templates (System/Instruction, Schema Context, Few-Shot Examples, Output Contract)
- Prompt versioning artifacts
- Schema-enum injection logic

All prompt content is versioned together as a single prompt version.
Segment B (Schema Context) is CONFIG-sourced, not versioned with the prompt.

See AES §2-8 for full prompt architecture specification.
