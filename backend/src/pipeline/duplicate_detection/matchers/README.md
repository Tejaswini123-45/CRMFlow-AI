# Duplicate Detection Matchers

Pluggable matcher implementations:
- Exact-match matcher (MVP)
- Fuzzy-match matcher (Category B, future)

The matcher interface allows swapping detection strategies without changing DEDUPE's core logic.
